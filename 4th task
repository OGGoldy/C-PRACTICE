#include <iostream>
#include <cmath>
using namespace std;
int main() {
    int count = 0;
    for(int i = 0; i <= 90; i++) {
        double rad = i * M_PI / 180.0;
        cout << sin(rad) << "\t";
        count++;
        if(count % 10 == 0)
            cout << endl;
    }
    return 0;
}
